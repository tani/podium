
tasks:
  - name: Install development tools
    command: brew install lua busted stylua deno
  - name: Start web app
    command: deno run -A deno/app.ts

vscode:
  extensions:
    - sumneko.lua

ports:
  - port: 8000
    visibility: private